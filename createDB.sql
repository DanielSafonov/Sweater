/* Не дописал */

/*
CREATE TABLE IF NOT EXISTS sweater."usr"(
  id  BIGINT,
  active BIT,
  password VARCHAR,
  username VARCHAR,
  first_name VARCHAR,
  last_name VARCHAR,
  email VARCHAR
);

CREATE TABLE IF NOT EXISTS sweater."usr_role"(
  usr_id BIGINT,
  roles VARCHAR
);


CREATE TABLE IF NOT EXISTS sweater."msgs"(
  id BIGINT,
  tag VARCHAR,
  text LONGBLOB,
  usr_id BIGINT,
  filename VARCHAR
);
*/